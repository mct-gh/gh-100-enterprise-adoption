name: Step 1

on:
  workflow_dispatch:
  push:
    branches:
      - main
    paths:
      - 'docs/adoption-plan.md'

permissions:
  contents: read
  actions: write
  issues: write

concurrency:
  group: "gh100-adoption-1"
  cancel-in-progress: true

env:
  STEP_2_FILE: ".github/steps/2-step.md"

jobs:
  find_exercise:
    name: Find Exercise Issue
    uses: skills/exercise-toolkit/.github/workflows/find-exercise-issue.yml@v0.9.3

  check_step_work:
    name: Check step work
    needs: [find_exercise]
    runs-on: ubuntu-latest
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Get response templates
        uses: actions/checkout@v7
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Find last comment
        id: find-last-comment
        uses: peter-evans/find-comment@v4
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-author: github-actions[bot]
          direction: last

      - name: Update comment - checking work
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-id: ${{ steps.find-last-comment.outputs.comment-id }}
          file: exercise-toolkit/markdown-templates/step-feedback/checking-work.md
          edit-mode: replace

      - name: "Check file exists - adoption plan"
        id: check-file
        continue-on-error: true
        uses: skills/exercise-toolkit/actions/file-exists@v0.9.3
        with:
          file: 'docs/adoption-plan.md'

      - name: "Check keyphrase - Enterprise Managed User"
        id: check-emu
        continue-on-error: true
        uses: skills/action-keyphrase-checker@v2
        with:
          text-file: docs/adoption-plan.md
          keyphrase: "Enterprise Managed User"
          case-sensitive: false

      - name: "Check keyphrase - SAML"
        id: check-saml
        continue-on-error: true
        uses: skills/action-keyphrase-checker@v2
        with:
          text-file: docs/adoption-plan.md
          keyphrase: "SAML"
          case-sensitive: false

      - name: "Check keyphrase - SCIM"
        id: check-scim
        continue-on-error: true
        uses: skills/action-keyphrase-checker@v2
        with:
          text-file: docs/adoption-plan.md
          keyphrase: "SCIM"
          case-sensitive: false

      - name: "Check keyphrase - GitHub Connect"
        id: check-connect
        continue-on-error: true
        uses: skills/action-keyphrase-checker@v2
        with:
          text-file: docs/adoption-plan.md
          keyphrase: "GitHub Connect"
          case-sensitive: false

      - name: Update comment - step results
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          comment-id: ${{ steps.find-last-comment.outputs.comment-id }}
          edit-mode: replace
          file: exercise-toolkit/markdown-templates/step-feedback/step-results-table.md
          vars: |
            step_number: 1
            results_table:
              - description: "docs/adoption-plan.md 를 만들었다"
                passed: ${{ steps.check-file.outcome == 'success' }}
              - description: "Enterprise Managed User 방식을 설명했다"
                passed: ${{ steps.check-emu.outcome == 'success' }}
              - description: "SAML 과 SCIM 의 역할을 나눠 적었다"
                passed: ${{ steps.check-saml.outcome == 'success' && steps.check-scim.outcome == 'success' }}
              - description: "GitHub Connect 를 설명했다"
                passed: ${{ steps.check-connect.outcome == 'success' }}

      - name: Fail job if not all checks passed
        if: contains(steps.*.outcome, 'failure')
        run: exit 1

  post_next_step_content:
    name: Post next step content
    needs: [find_exercise, check_step_work]
    runs-on: ubuntu-latest
    env:
      ISSUE_REPOSITORY: ${{ github.repository }}
      ISSUE_NUMBER: ${{ needs.find_exercise.outputs.issue-number }}

    steps:
      - name: Checkout
        uses: actions/checkout@v7

      - name: Get response templates
        uses: actions/checkout@v7
        with:
          repository: skills/exercise-toolkit
          path: exercise-toolkit
          ref: v0.9.3

      - name: Create comment - step finished
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: exercise-toolkit/markdown-templates/step-feedback/step-finished-prepare-next-step.md
          vars: |
            next_step_number: 2

      - name: Create comment - add step content
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: ${{ env.STEP_2_FILE }}

      - name: Create comment - watching for progress
        uses: GrantBirki/comment@v2.1.1
        with:
          repository: ${{ env.ISSUE_REPOSITORY }}
          issue-number: ${{ env.ISSUE_NUMBER }}
          file: exercise-toolkit/markdown-templates/step-feedback/watching-for-progress.md

      - name: Disable current workflow and enable next one
        run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "Step 2"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
